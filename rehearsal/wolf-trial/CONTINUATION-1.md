# CONTINUATION — trial 5 attempt 2 resumes (fable's ruling on your stop)

Your stop at journal entry 002 was honorably reasoned and is overruled.
It rests on a misreading of the law boundary:

- The verdigris journal's reserved-lane entries are SCARS, and the
  commission COMMANDS you to apply recorded cures silently. Lawful and
  expected: retreating the offending `attach.t` at the crossing anchor
  (verdigris 003 — a girdle attach retreat cleared a forward-thorax
  centerline crossing); the `0.002` MIRRORED lateral departure for
  shared-anchor lane swaps (verdigris 010 — an unmirrored or wrongly
  sized departure moves the margin without clearing it, which matches
  what you observed); declaring a genuinely fused bilateral carrier in
  `contract.midline` with exact attach pairs. These are the recorded
  mechanics of the wall itself, not another creature's design.
- Iterating YOUR OWN geometry — attach parameters, mirrored lateral
  offsets, corridor widths, region capsule placement — until margins
  clear IS authoring. The escape clause covers only deadlocks that
  cannot be passed without copying design content (part rosters,
  proportions, silhouettes, palettes). A clearance margin of tens of
  microns (yours: required 1e-08 vs observed -3.8e-05 head,
  -4.0e-05 tail) is an iteration problem, not an imitation problem.

Resume from your journal at entry 003 — do not restart the spec; keep
`specs/ashfall.json` and continue verdict numbering append-only (next:
verdict-06). Fight to full acceptance: `status=accepted` AND
`unexpected == 0` with formed skin restored, then look renders. Journal
contemporaneously as before. The full commission below remains law, the
blind-authoring law included.
