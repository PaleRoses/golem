# COMMISSION — wolf authoring trial (trial 5, attempt 2)

Author a new creature on the golem session surface: a WOLF. Cursorial
quadruped predator — deep chest, tucked waist, four digitigrade legs, a
straight expressive tail, a long muzzle. The bauplan is deliberately
simple: this trial measures whether the amended surface (wave 5 strict
acceptance + help density + the formation vocabulary) carries an author
to acceptance unaided on a known shape. The friction journal is as much
the deliverable as the creature.

## THE BLIND-AUTHORING LAW (Rosalia's ruling; attempt 1 was convicted
## of violating it and its artifacts quarantined)

You author from ZERO. You may not look at how others did it:

- NEVER open, read, diff, or compare against ANY spec in `specs/` —
  not `sable_stride.json`, not any other creature, not the quarantined
  `attempt-1-contaminated/` directory (which is radioactive: a prior
  attempt cloned Sable's part list and the trial was killed for it).
- NEVER reuse another creature's part ids, dimensions, proportions,
  stance, contact graph, or palette. Every number in your spec comes
  from your own frame probes and your own judgment of a wolf's body.
- The FRICTION journals you are directed to read are for SCARS ONLY —
  mechanical walls and their cures (tooling invocations, coordinate
  conventions, sequencing laws). Any design content in them (a
  creature's shapes, sizes, part rosters) is off-limits: note the wall,
  never the anatomy.
- Your creature's name must not be "Frostfang" (burned by attempt 1)
  and its spec must be a NEW file under `specs/`.

If you cannot reach acceptance without imitation, that is the finding:
journal the wall honestly and stop. A blind failure is worth more than
a plagiarized success.

## The contemporaneous-friction law (standing, Rosalia's ruling)

FRICTION.md entries land WHEN the wall is hit — within a transaction or
two, never deferred to a closing rite. Trial 4's author end-loaded her
journal, hung, and the entire interior record died with her. Verdicts
record the kernel's side; the journal records the author's interior —
that is the harvest data. Write the entry, then continue.

## Scope & conventions

- Spec at `specs/<name>.json` — christen the wolf yourself. All trial
  artifacts live in `rehearsal/wolf-trial/` (this directory):
  `FRICTION.md` (contemporaneous, exact command + verdict fragment at
  the moment of confusion), `verdict-NN.json` append-only, `base.json`,
  look renders — per prior trials' conventions.
- Carry the scars: read `rehearsal/scree-maiden-trial/FRICTION.md`,
  `rehearsal/verdigris-trial/FRICTION.md`,
  `rehearsal/wyvern-trial/FRICTION.md`, and
  `rehearsal/formation-trial/FRICTION.md` FIRST. A wall those journals
  already name is a scar — apply the recorded cure silently; only NEW
  walls earn entries. Build in stages (verdigris 000): skeleton +
  carrier flesh and vascular acceptance first, decorative flesh next,
  attach pass, tint last. Frame-probe every multi-bone chain before
  fleshing it; remember the axial rest_dir law (verdigris 001) — the
  tail is where it bites.
- The formation vocabulary exists and is settled — use it where the
  form honestly wants it: `"formation": "skeleton_integral"` for
  fusiform muscle bellies, `"formation": "static_implicit_relaxation"`
  skin (uniform thickness; quadrupeds ARE lawful now — the global-ray
  wall is cured), `local_blend` where a junction bulge offends. A carve
  for the mouth if the look calls for it. Nothing exotic is demanded.
- Acceptance: the `session` verdict is the canonical oracle — accepted
  status AND `unexpected == 0` (kernel-enforced; a warning-severity
  rejection binds nothing). `check` should agree; if it disagrees, that
  is a journal entry. The four-foot stance must clear `env_balanced`
  honestly — no undeclared props.
- Environment: invoke the kernel via `.venv/bin/python -m golem ...`
  directly — `uv run` panics under the sandbox (verdigris 012). Set
  `GOLEM_CIRCUIT_CACHE_COLD=1` only while iterating vascular seeds
  (verdigris 002); leave it unset otherwise — the warm field memo is
  the edit loop's speed and is lawful.
- Do NOT modify the kernel, session, senses, or contract code. Trials
  are authoring-only; if the surface is wrong, the journal entry is the
  deliverable — author around the wall honestly and record it. No git
  commands of any kind.
