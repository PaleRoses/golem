# CLOSER COMMISSION — land Sable Stride

The original author (a weaker model) descended from 60 unexpected diagnostics
to 1 across 21 session transactions, then hung without journaling. You are the
closer. Read `COMMISSION.md` (the governing law — fences, oracles, honesty
clause all still bind you), then finish the creature from its current state:
`specs/sable_stride.json`, verdicts `verdict-01..21.json` (append-only — your
first verdict is `verdict-22.json`).

Goal: `session` accepted with 0 unexpected diagnostics; cold `check` and
`look` agree; final shaded renders in `rehearsal/formation-trial/look-final/`;
all three formation tokens (`local_blend`, `skeleton_integral`,
`static_implicit_relaxation`) present in the accepted creature per the
original commission.

The two live fronts, from the verdict record:
- `body.SkinFormationAnatomyObstruction @ anatomy/integument_layers` — the
  formed-skin uniform-cover law (every anatomy region, one common thickness).
- `body.anomaly.deep_burial` on muscle~mass pairs (`chest_mass~shoulder_sling`
  and kin) — formed bellies buried inside torso masses. Repair the geometry
  honestly (slimmer masses, repositioned bellies); do not delete the formed
  muscles to silence the anomaly.

Write `FRICTION.md` yourself: walls 001+ from the verdict record (attribute
the original author's descent and hang honestly — you inherit receipts, not
credit), then your own closing entries. New walls only; known law limits are
listed in COMMISSION.md and are not discoveries.
