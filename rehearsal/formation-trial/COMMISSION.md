# COMMISSION — Formation Trial (trial 4): the first creature of the formed-surface era

You are the sole author. You author a creature; you do not modify the kernel.
This is the first authoring trial against the surface-formation vocabulary
sealed this morning (certified composition, skeleton-integral muscle
formation, solved skin). Your task is to exercise it honestly.

## The creature

A lean cursorial quadruped — a runner (greyhound-like proportions, deep
chest, long limbs, long counterweight tail). You name it. Its body must
exercise ALL THREE new opt-in tokens:

1. `local_blend` composition at TWO or more tissue junctions (shoulder,
   haunch — places where the legacy smin would bulge).
2. `"formation":"skeleton_integral"` on FOUR or more muscles (long fusiform
   bellies along limbs and tail are the natural fit).
3. `"formation":"static_implicit_relaxation"` skin — the formed integument
   over the full anatomy cover.

Plus the standing menagerie honesty: a four-foot stance with a real
environment-contract margin, at least two material classes, and clean
proprioception (ANOMALY 0).

## Law

- Discover the vocabulary from the teaching surface itself: the contract
  primer/schema, `.venv/bin/python -m golem --help` and subcommand help.
  Typed rejections ARE your steering — repair the declaration the witness
  names; never silently delete authored intent to make a verdict go quiet.
- Canonical acceptance oracle: `session`. A run is accepted by its `status`
  and diagnostics, never by exit code. Cold `check` and `look` must agree
  with `session` at the end (oracle-agreement is law now); a disagreement is
  a wall, not a workaround opportunity.
- Environment: direct interpreter only —
  `GOLEM_CIRCUIT_CACHE_COLD=1 .venv/bin/python -m golem ...`.
- Write ONLY `specs/<your-creature>.json` and files under
  `rehearsal/formation-trial/`. Never touch other trial directories, the
  kernel, tests, or docs. No git commands of any kind.
- Journal: `rehearsal/formation-trial/FRICTION.md`, new walls only —
  inherited scars from prior trials are not discoveries. Verdicts are
  append-only: `verdict-01.json`, `verdict-02.json`, ... Save final shaded
  renders under `rehearsal/formation-trial/look-final/`.

## Known law limits — NOT walls, do not journal them as discoveries

- The muscle formation family is deliberately fusiform-only: circular
  sections, branch-free near-straight centerlines. Anisotropic sheets,
  branches, and exotic profiles reject with a typed witness — that is the
  law working, not a bug. Stay inside the family.
- Formed skin requires ONE uniform thickness across the entire anatomy
  cover; mixed legacy/formed regions and unequal thicknesses reject typed.
- The formed-skin solve is expensive (~80x legacy on small graphs). Keep
  authoring-loop resolutions modest; spend resolution only on final looks.
- A13d (web-blind vasculature) remains known-open. Not your problem: this
  bauplan has no webs.

## Honesty clause

If an obstruction cannot be discharged lawfully within the closed families,
record it in FRICTION.md and deliver the best fully-accepted creature without
the blocked feature. An honest partial beats a dishonest total. Your final
FRICTION entry states plainly which of the three tokens reached the accepted
creature and which did not, with the verdict receipts.
