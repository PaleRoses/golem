# FRICTION JOURNAL — cinderwake (true wyvern) authoring trial

Trial 3. Sole author: Blue Rose. First creature trial against the wave-3
amended session surface. New walls only: the scree-maiden and verdigris journals
are inherited scars, not discoveries to be repackaged as novelty. Verdict JSONs
are append-only in this directory; `session` is the canonical acceptance oracle.

## 000 — protocol and inherited scars, before authoring

- Creature name: Cinderwake. Bauplan: two weight-bearing hind legs; forelimbs
  are the only wings; long axial counterweight tail; no separate arms and no
  undeclared wing-knuckle supports.
- Staged descent: skeleton plus carrier flesh and vasculature; decorative
  non-carrier flesh and 3+-anchor wing webs; attach declarations; carves and
  appearance last.
- Every multi-bone chain will be frame-probed before flesh. Tail continuations
  use local +z after the root reversal. Blob sizes remain half-extents.
- Direct interpreter only:
  `GOLEM_CIRCUIT_CACHE_COLD=1 .venv/bin/python -m golem ...`.
- A run is not accepted by exit status. I will read `status`, diagnostics,
  margins, binding constraint, and the visible check headline. `check` disagreeing
  with `session` is a new wall; web-blind vasculature is the known-open A13d wall.

## 001 — `FailedGluing` has no authoring restriction

Command:
`GOLEM_CIRCUIT_CACHE_COLD=1 .venv/bin/python -m golem session rehearsal/wyvern-trial/base.json specs/cinderwake.json --all`

Saved: `verdict-02.json`. Verdict fragment:
`status=rejected`; `vascular.FailedGluing @ closed-vascular-field`;
`reason="NonConvergentBalance @closed-vascular-balance: solver_info=10920"`;
`binding_constraint=null`; `help.operations=[]`; no violated margins.

This is a new wall. Wave 3 now gives excellent body addresses and segment
coordinates for geometric vascular obstructions, as `verdict-01.json` proved,
but a gluing failure still collapses to a numerical solver code with no local
section, overlap, circuit, demand, or authoring address. The only adjacent edit
was the honest ontology repair from three perfused finger leaves to one perfused
middle strut plus two `handle` fingers. The surface cannot say whether the
obstruction belongs to topology, conditioning, demand, or a solver budget, so
the next move is necessarily a controlled author-side probe rather than a
restriction-directed repair.

## 002 — a sensed web has no writable attach address

Command:
`GOLEM_CIRCUIT_CACHE_COLD=1 .venv/bin/python -m golem check /tmp/cinderwake-attached-v1.json --all`

Verdict fragments:
`unintended_fusion @ wing_forearm~cinderwake_wing_membrane`;
`unintended_fusion @ wing_digit_lead~cinderwake_wing_membrane`;
then, after copying those exact pairs into `contract.attach`,
`[bad_contract_attach] ... unknown part ids ['cinderwake_wing_membrane']`.

This is a new wall. Proprioception promotes the web into the part topology and
uses its ID in an actionable-looking anomaly address, but contract validation
does not admit that same ID in the attach vocabulary. The diagnostic therefore
names an interface the author cannot declare. Anchor-to-web attachments are
derived automatically, so the honest workaround is geometric rather than
declarative: restrict every membrane anchor span away from the wrist until the
web no longer intersects non-anchor carrier flesh. The rejected attach pairs
are removed rather than preserved as ceremonial intent.

## 003 — canonical acceptance disagrees with an immediate cold check

Canonical command:
`GOLEM_CIRCUIT_CACHE_COLD=1 .venv/bin/python -m golem session rehearsal/wyvern-trial/base.json specs/cinderwake.json --all`

Saved: `verdict-04.json`. Verdict fragment:
`status=accepted`; `diagnostics_summary.unexpected=0`; no errors; no violated
margins; `binding_constraint=contract:env_integrity` satisfied at `1 == 1`.

Immediate independent command:
`GOLEM_CIRCUIT_CACHE_COLD=1 .venv/bin/python -m golem check specs/cinderwake.json --all`

Contradictory headline:
`ASSEMBLY acceptance: REJECTED`;
`[assembly.unexpected_compile_exception]`;
`observed="TypeError"`;
`message="compose_union() takes 1 positional argument but 4 were given"`.
`golem look` then crashed at the same engine call instead of returning a typed
look obstruction.

This is the commission's explicit B1 failure: the canonical oracle accepted the
document that the immediate cold secondary oracle rejected. Worse, the
secondary rejection is not a typed authoring obstruction and offers no
operation. I will not modify kernel code in an authoring trial. The remaining
lawful move is to restrict the authored composition vocabulary until cold
`session`, cold `check`, and `look` descend to the same result, preserving this
accepted verdict as the append-only evidence of disagreement.

Source restriction proved that no authoring field can clear this failure.
`golem/kernel/engine/algebra.py::compose_union` now accepts one
`CompositionProblem`; live callers in
`golem/kernel/engine/compile.py::_compose_sampled_region` still pass
`field, sampled, operator, blend_radius`. Every multi-part creature therefore
reaches the same arity error. Those kernel edits are outside this authoring-only
commission and were not modified.

The pre-drift cold check of `/tmp/cinderwake-tinted-v2.json` had already
reported `ASSERT 0 FAIL / 2 pass`, `ANOMALY 0`, `VASCULAR feasibility:
ACCEPTED`, and `ASSEMBLY acceptance: ACCEPTED`; its three shaded look renders
also completed. JSON equality confirms that file, `specs/cinderwake.json`, and
the accepted document embedded in `verdict-04.json` are semantically identical
(the only textual differences are decimal spellings such as `0.010` versus
`0.01`). Those contemporaneous renders are preserved under `look-final/`.

## 004 — acceptance receipt

`verdict-04.json` is the canonical accepted transaction:
`status=accepted`; 58 expected diagnostics, 0 unexpected; no errors; no
violated margins. `env_balanced` observed `+0.027` against the honest
`>= +0.020` two-foot requirement; `env_integrity` observed exactly one
component.

The semantically identical pre-drift cold check reported:
`ASSERT 0 FAIL / 2 pass`; `ANOMALY 0`; four of four closed exchange beds;
`VASCULAR feasibility: ACCEPTED`; `ASSEMBLY acceptance: ACCEPTED`.
The complete transcript is preserved as `check-accepted.txt`.

Delivered form: a true bipedal wyvern with no foreleg branch, one perfused
wing-tip carrier route, three `handle` wing fingers, one three-anchor
`non_carrier` web, explicit non-carrier keratin, mirrored crown handles, three
skull carves, opaque/translucent/emissive material classes, two mirrored hind
heel contacts, and a long perfused counterweight tail. Final shaded views:
`look-final/cinderwake_{front,side,top}.png`.

The later live-kernel arity break and oracle disagreement remain recorded in
003; authoring-only scope forbids disguising that external source drift as a
creature edit.
