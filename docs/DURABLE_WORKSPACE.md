# Durable GOLEM workspace

`agent-validation` is the reconstructed semantic baseline. Feature campaigns branch from it under `sol/*` and must retain machine-readable quality, topology, incremental-execution, and vascular receipts.

The current distal-character campaign is `sol/distal-character-vascular-quality`.

Generated meshes and image sheets are CI/release artifacts. Source, tests, authored creature specifications, stable metric definitions, and JSON regression receipts are committed.
